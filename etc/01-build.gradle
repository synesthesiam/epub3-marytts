plugins {
  id "de.dfki.mary.voicebuilding.marytts-kaldi-mfa" version "0.3.6"
}

// Default: Locale.US
// convertTextToMaryXml.locale = Locale("<LANGUAGE>")

prepareForcedAlignment.wavDir = file("wav")
